server {
    listen 8081;
    host localhost;
    
    # 基本設定
    client_max_body_size 1M;
    
    location / {
        root /var/www/secondary;
        index index.html;
        allow_methods GET POST;
        isCGI true;
    }
}
